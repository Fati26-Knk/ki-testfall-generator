# Frontend API configuration
# Local development points to the backend on port 8000.
# The backend serves its API routes from the root path, so no `/api/v1` suffix here.
VITE_API_URL=http://localhost:8000
